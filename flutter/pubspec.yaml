name: dptp_client_sdk
description: SDK for DPTP
version: 0.0.9
homepage: https://github.com/PositionExchange/dptp-client-sdk/tree/main/flutter

environment:
  sdk: ">=2.17.0 <=3.0.5"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  ffi: ^2.0.1
  flutter_rust_bridge: '>=1.38.0 <1.38.1'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
